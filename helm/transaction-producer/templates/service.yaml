apiVersion: v1
kind: Service
metadata:
  name: {{ include "transaction-producer.fullname" . }}
  labels:
    {{- include "transaction-producer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transaction-producer.selectorLabels" . | nindent 4 }}

