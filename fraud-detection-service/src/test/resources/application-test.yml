spring:
  application:
    name: fraud-detection-service-test
  autoconfigure:
    exclude:
  cloud:
    gcp:
      logging:
        enabled: false

cloud:
  provider: local

fraud:
  rules:
    large-amount:
      threshold: 10000
    suspicious-accounts: ACCT001,ACCT666,ACCT999
    rapid-fire:
      max-per-minute: 5

logging:
  level:
    com.hsbc.fraud: DEBUG
    root: WARN

